<!DOCTYPE html>
<html>
    <head>
        <title> The Yelpcamp </title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
    <style type="text/css" rel="stylesheet">
    .navbar{
       padding: 0 auto; 
       margin: 0 auto;
       height: 60px;
       background-color: #E5E4E2;
   }
   #com{
       margin-top: 8px;
   }
   #link{
       float: right;
       padding-top: 17px;
       padding-right: 20px; 
    }
    #middlelink{
        padding-right: 22px; 
        padding-left: 22px;
    }
    #alink{
        color: black;
    } 
    #alink:hover{
        color: black;
        text-shadow: grey;
        text-decoration: none;
    }
    #textyelpcamp{
        font-size: 20px;
        color: grey;
        padding-top: 17px;
        padding-left: 20px;
        position: absolute;
    }
    #header{
        margin-top: 20px;
    }
    .title{
        padding-top: -10px;
    }
    #thumb{
        padding: 0px; 
    }
    #cap{
        padding: 9px;
    }
    #show-img{
        width: 100%;
    }
    .signIn{
       position: relative;
       right: 120px;
    }
    .outlink{
       position: absolute;
       float: right;
       bottom: 24px;
       right: 60px;
    }
    #alertcontainer{
        margin-top: 15px;
        margin-bottom: -14px;
    }
    </style>
    </head>   
<body>
    <div class="navbar">
        <span id="textyelpcamp"> YelpCamp </span>
         <div id="link">
            <% if(!currentUser){  %>
                <a id="alink" href="/login"> Login </a>
                <span id="middlelink"> <a id="alink" href="/register"> Sign Up</a> </span>
            <% } else{ %>
                <p class="signIn">Signed In As <%= currentUser.username %> </p> 
                <a class="outlink" id="alink" href="/logout"> Logout </a> 
            <% } %>
        </div>
    </div>
    <div id="alertcontainer" class="container">
       <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0){ %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>    
    </div>